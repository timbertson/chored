/*
 * parse imports
 *
 * bump strategy
 *  - github
 *  - deno land
 *  - std
 *
 * For github, we can specify a branch after the hash
 * https://.../mod.ts#master
 *
 * If no branch, track latest tag || release, whichever is higher
 * (TODO: should we just track tags? When are there releases without tags?)
 */
