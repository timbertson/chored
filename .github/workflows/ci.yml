# NOTE: This file is generated by chored

'on':
  push:
    branches:
      - main
      - master
  pull_request: {}
jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: timbertson/chored-init-action@v1
      - name: precommit
        run: ./chored precommit
        env: {}
